from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='Processing and analysis of the baseline survey data collected in Kananga, DRC',
    author='JF',
    license='',
)
